# Contributing

Use Node 24, run `npm ci`, `npm run fixtures`, `npm run check`, and `npm run test:e2e` before opening a pull request. Please include synthetic fixtures for detection changes. Never commit gameplay captures, device paths, tokens, or generated dependency folders.
